[project]
name = "skypulse-mcp"
version = "0.1.0"
description = "Simple MCP server for weather"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.10.1",
]
