[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00dialoger"]

[project]
name = "r00dialoger"
version = "0.1.5"
requires-python = ">=3.10"
dependencies = [
    "pillow"
]

