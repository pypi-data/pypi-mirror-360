
class AmpLearn:
  def __init__(self) -> None:
    """这个包有点老 
    https://amp.readthedocs.io/en/latest/installation.html
    Amp是基于python的，旨在与原子模拟环境（ASE）紧密集成。由于这种紧密的集成，Amp与每个主要的电子结构计算器兼容，并且可以随时访问所有标准的原子方法，例如结构优化或分子动力学。在其最基本的形式中，它几乎没有要求：
    """
    pass

  def install(self):
    """
    # 创建 Rosetta 2 的 Conda 环境
    CONDA_SUBDIR=osx-64 conda create -n rosetta_env python=3.8
    conda activate rosetta_env
    conda config --env --set subdir osx-64

    # 然后尝试安装
    pip install amp-atomistics
    """
    pass
