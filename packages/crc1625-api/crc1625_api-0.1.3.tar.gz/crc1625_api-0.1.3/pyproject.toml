[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crc1625_api"
version = "0.1.3"
description = "Python client for querying and downloading data from the MatInf VRO API"
readme = "README.md"
authors = [{ name = "Doaa Mohamed", email = "doaamahmoud262@yahoo.com" }]
requires-python = ">=3.7"
dependencies = [
    "requests",
    "pandas",
    "openpyxl"
]
