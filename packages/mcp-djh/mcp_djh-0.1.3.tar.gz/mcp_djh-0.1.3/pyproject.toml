[project]
name = "mcp-djh"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dartou", email = "" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.4",
    "requests",
]

[project.scripts]
mcp-djh = "mcp_djh:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
