[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00richi"]

[project]
name = "r00richi"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "rich",
    "pyfiglet"
]

