[project]
name = "shipyard-email"
version = "0.3.2a9"
description = "Simplified blueprints for building data pipelines with Email"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.3",
    "shipyard-bp-utils>=1.3.1",
    "shipyard-templates>=0.10.1a0",
    "typing-extensions>=4.11.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "python-dotenv>=1.0.1",
]
[tool.uv]
package = true