[project]
name = "autoad"
version = "0.1.0"
description = "A simple automatic algorithm design tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autoad = "autoad.main:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
