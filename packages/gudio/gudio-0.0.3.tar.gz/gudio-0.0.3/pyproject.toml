[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"

[project]
name = "gudio"
authors = [{name = "calcuis", email = "info@calcu.io"}]
description = "gudio toolkit"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]
dependencies = ["librosa", "s3tokenizer", "omegaconf"]

[project.urls]
GitHub = "https://github.com/calcuis/gudio"