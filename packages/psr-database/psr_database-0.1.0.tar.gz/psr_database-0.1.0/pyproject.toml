[project]
name = "psr-database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raphasampaio", email = "raphaelaraujosampaio@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/psr"]