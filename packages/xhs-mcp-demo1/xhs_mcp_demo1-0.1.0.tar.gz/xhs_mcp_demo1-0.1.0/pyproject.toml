[project]
name = "xhs-mcp-demo1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
xhs-mcp-demo1 = "xhs_mcp_demo1:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
