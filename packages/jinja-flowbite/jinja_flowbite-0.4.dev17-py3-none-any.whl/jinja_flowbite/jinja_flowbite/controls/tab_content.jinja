{% macro render(id) %}

<div class="hidden border p-4 rounded-lg bg-gray-50 dark:bg-gray-800" id="tab-content-{{id}}" role="tabpanel" aria-labelledby="{{id}}-tab">
    {% if caller %}
        {{ caller() }}
    {% endif %}
</div>

{% endmacro %}