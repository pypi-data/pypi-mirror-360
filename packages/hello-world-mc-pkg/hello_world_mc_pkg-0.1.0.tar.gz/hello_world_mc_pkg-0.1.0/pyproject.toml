[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hello_world_mc_pkg"
version = "0.1.0"
description = "A simple hello world package."
authors = [
    {name = "Meng-Chieh Ling", email = "mengchiehling@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}