[project]
name = "georeport"
version = "0.3.0"
description = "Python wrapper for the Open311 GeoReport v2 API"
authors = [
    {name = "Pramod Anandarao"}
]
license = {text = "Apache License 2.0"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests (>=2.32.4,<3.0.0)",
    "xmltodict (>=0.14.2,<0.15.0)",
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.1"
flake8 = "^7.3.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
