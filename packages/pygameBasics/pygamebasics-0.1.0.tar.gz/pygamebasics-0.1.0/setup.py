from setuptools import setup, find_packages

setup(
    name="pygameBasics",
    version="0.1.0",
    description="Auto-uploaded package",
    packages=find_packages(),
    python_requires=">=3.6"
)
