[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "r00android_fake"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = [
    "r00arango_connector",
    "requests[socks]",
]

[tool.hatch.build.targets.wheel]
packages = ["src/r00android_fake"]