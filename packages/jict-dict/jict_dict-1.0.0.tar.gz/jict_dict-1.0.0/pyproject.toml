[project]
name = "jict-dict"
version = "1.0.0"
description = "JICT. a powerfull bidirectional dict"
readme = "README.md"
requires-python = ">=3.6"
authors = [
  { name = "eljeiforeal"}
]
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
