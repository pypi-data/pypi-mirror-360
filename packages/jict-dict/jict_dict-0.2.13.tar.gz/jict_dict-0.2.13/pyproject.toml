[project]
name = "jict-dict"
version = "0.2.13"
description = "JIST. a powerfull bidirectional dict"
readme = "README.md"
requires-python = ">=3.6"
authors = [
  { name = "eljeiforeal"}
]
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
