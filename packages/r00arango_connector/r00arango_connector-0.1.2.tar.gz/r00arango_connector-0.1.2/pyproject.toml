[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00arango_connector"]

[project]
name = "r00arango_connector"
version = "0.1.2"
requires-python = ">=3.10"
dependencies = [
    "python-arango",
    "r00secret",
]
