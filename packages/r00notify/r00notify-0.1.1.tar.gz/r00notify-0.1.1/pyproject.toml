[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00notify"]

[project]
name = "r00notify"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = [
    "pyTelegramBotAPI",
]

