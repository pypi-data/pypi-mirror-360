def operands(operand1,operand2):
    if  operand1==operand2:
        print("{}and {} operands are same".format(operand1,operand2))
    else:
        print("{}and{} operands are not same".format(operand1,operand2))
operands(10,10)        

