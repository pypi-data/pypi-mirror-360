{% for resource in resources -%}
resource "aws_api_gateway_resource" "{{ resource.restApiId }}_{{ resource.id }}" {
  rest_api_id = "{{ resource.restApiId }}"
  parent_id   = "{{ resource.parentId }}"
  path_part   = "{{ resource.pathPart }}"
}

{% if not loop.last %}

{% endif -%}
{%- endfor %}
