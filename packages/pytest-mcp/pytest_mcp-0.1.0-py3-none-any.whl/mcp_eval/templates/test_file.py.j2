# Generated by mcp-eval. Do not edit manually.
# Test file for server: {{ server_name }}

from mcp_eval import task
from mcp_eval.assertions import contains

{% for test in tests %}
@task(description="{{ test.description }}", server="{{ server_name }}")
async def {{ test.test_name }}(agent, session):
    """
    {{ test.description }}
    """
    objective = "{{ test.objective }}"
    response = await agent.llm.generate_str(objective)
    
    # Assertions
    {% for assertion in test.assertions %}
    contains(response.lower(), "{{ assertion }}", session=session)
    {% endfor %}
{% endfor %}