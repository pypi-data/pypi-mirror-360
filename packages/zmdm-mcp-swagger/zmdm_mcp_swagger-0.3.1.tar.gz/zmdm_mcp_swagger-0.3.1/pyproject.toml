[project]
name = "zmdm-mcp-swagger"
version = "0.3.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "xiaojy", email = "735516422@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
    "requests>=2.31.0",  # 添加requests依赖
]

[project.scripts]
zmdm-mcp-swagger = "zmdm_mcp_swagger:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
