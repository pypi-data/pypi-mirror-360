[project]
name = "calculadora-avancada-color"
version = "1.0.0"
description = "Uma calculadora avançada com cores no terminal"
authors = [
  { name = "Seu Nome", email = "seu@email.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://github.com/Lgb145483/calculadora"
Repository = "https://github.com/Lgb145483/calculadora"
Changelog = "https://github.com/Lgb145483/calculadora/blob/main/CHANGELOG.md"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
target-version = ['py311']

[tool.pytest.ini_options]
addopts = "--maxfail=1 --disable-warnings --cov=."
testpaths = ["tests"]