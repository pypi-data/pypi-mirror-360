[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00system"]

[project]
name = "r00system"
version = "0.1.1"
requires-python = ">=3.10"
