[tool.poetry]
name = "py-pd-stepper"
version = "0.1.1"
description = ""
authors = ["Thiago Meyer <tiagmey@gmail.com>"]
readme = "README.md"
packages = [{include = "pd_stepper", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
pyserial = "^3.5"

[tool.poetry.group.dev.dependencies]
mypy = "^1.4.1"
pytest = "^7.4.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[[tool.mypy.overrides]]
ignore_missing_imports = true