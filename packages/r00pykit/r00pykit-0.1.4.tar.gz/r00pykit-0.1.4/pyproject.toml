[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00pykit"]

[project]
name = "r00pykit"
version = "0.1.4"
requires-python = ">=3.10"
dependencies = [
    "r00logger",
]

