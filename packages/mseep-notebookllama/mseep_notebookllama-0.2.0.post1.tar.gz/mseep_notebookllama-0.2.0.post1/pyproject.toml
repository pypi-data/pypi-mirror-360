[tool.mypy]
disable_error_code = [
    "import-not-found",
    "import-untyped",
]

[tool.pytest.ini_options]
pythonpath = [
    "src",
]

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"
