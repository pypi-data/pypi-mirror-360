[project]
name = "SwedishFurniture"
version = "1.0.0"
description = "Furniture name generator like a popular Swedish company does it."
authors = [{ name = "Mikhail" }]
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
