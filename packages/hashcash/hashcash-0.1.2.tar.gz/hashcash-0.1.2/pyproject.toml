[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hashcash"
version = "0.1.2"
authors = [
    {name = "David Mertz", email="unknown@invalid.tld"},
]
maintainers= [
    {name = "Scott Alfter", email = "scott@alfter.us"},
]
description = "Hashcash"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
hashcash = "hashcash:main"
