[project]
name = "nuvemshop-client-python"
version = "0.2.5" # Incremente a versão
description = "Um cliente Python robusto e intuitivo para a API da Nuvemshop."
readme = "README.md"
requires-python = ">=3.13"
authors = [
  { name="Bruno Vidal", email="brunovidal@lojabibelo.com.br" },
]


dependencies = [
    "requests>=2.32.3",
]
[tool.setuptools]
package-dir = {"" = "src"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "build>=1.2.2.post1",
    "twine>=6.1.0",
]
