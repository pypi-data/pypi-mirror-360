[tool.poetry]
name = "somanath-python-library"
version = "0.1.0"
description = "A simple Python library with addition, subtraction, and greeting functions."
authors = ["somanath <shindesomanath111@gamil.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "my_python_library"}]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.group.dev.dependencies]
pytest = "^6.0"