[project]
name = "coop-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
coop-mcp-demo = "coop_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
