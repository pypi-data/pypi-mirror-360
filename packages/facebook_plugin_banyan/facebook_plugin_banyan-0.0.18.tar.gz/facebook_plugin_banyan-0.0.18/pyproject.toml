[project]
name = "facebook_plugin_banyan"
version = "0.0.18"
description = ""
authors = [
    {name = "chuck yang",email = "yzhang.baby@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11, <4.0"
dynamic = ["dependencies"]

[project.entry-points."banyan.plugins"]
facebook_account_promotion = "facebook_plugin_banyan.facebook_plugin:AccountPromotionPlugin"

[tool.poetry]
packages = [{include = "facebook_plugin_banyan", from = "src"}]


[tool.poetry.dependencies]
xcmap = ">=0.0.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
