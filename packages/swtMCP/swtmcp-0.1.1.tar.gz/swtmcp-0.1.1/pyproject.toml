[project]
name = "swtMCP"
version = "0.1.1"
description = "A package for credit risk modeling with various tools."
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "mcp[cli]>=1.10.1",
    "openai",   # Add any extra dependencies
    "requests", # For API requests
    "sklearn",  # For model evaluation
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
