[project]
name = "sanyamuvpackage"
version = "0.1.1"
description = "Sanyam Testing app"
readme = "README.md"
authors = [
    { name = "Sayyam-Karnavat", email = "sanyam.karnavat5@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.1",
]

[project.scripts]
sanyamuvpackage = "sanyamuvpackage:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
