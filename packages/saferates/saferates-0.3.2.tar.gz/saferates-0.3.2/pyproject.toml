[project]
name = "saferates"
version = "0.3.2"
description = "Discord API toolkit for user tokens with token onliner functionality."
authors = [{name="DriizzyyB", email="driizzyyboosts@gmail.com"}]
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.8"
dependencies = ["httpx>=0.25.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"