[project]
name = "pyaitrainer"
version = "0.1.1"
description = "Minimal transformer-based character-level trainer"
authors = [{ name = "I'm_caca", email = "bababui544@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = ["torch"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
