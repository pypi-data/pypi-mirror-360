[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "df_anonymizer"
version = "0.1.0"
description = "A simple data anonymization library with masking, data perturbation, data permutation, data suppression, and pseudonymization functions."
authors = [
    { name="Tan Shih Jen" }
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]