[project]
name = "zapcap-mcp-server"
version = "0.1.5"
description = "MCP server for ZapCap.ai video processing and caption generation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.10.1",
    "pydantic>=2.11.7",
]

[project.scripts]
zapcap-mcp-server = "src.server:main"
