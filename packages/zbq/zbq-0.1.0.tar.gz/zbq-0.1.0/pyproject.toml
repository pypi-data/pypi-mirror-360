[project]
name = "zbq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FrocketGaming", email = "frocketgames@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "google-cloud-bigquery>=3.34.0",
    "google-cloud-bigquery-storage>=2.32.0",
    "google-cloud-storage>=3.1.1",
    "polars>=1.31.0",
]

[project.scripts]
zbq = "zbq:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
