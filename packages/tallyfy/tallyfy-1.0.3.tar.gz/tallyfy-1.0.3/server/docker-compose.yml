version: '3.8'

services:
  mcp-server:
    network_mode: "host"
    container_name: "mcp-server"
    build: .
    image: "tallyfy-mcp-server"
    ports:
      - "9000:9000"
    restart: unless-stopped