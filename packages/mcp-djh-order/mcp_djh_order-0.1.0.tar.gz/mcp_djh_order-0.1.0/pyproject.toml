[project]
name = "mcp-djh-order"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dartou", email = "" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.10.1",
    "requests",
]

[project.scripts]
mcp-djh-order = "mcp_djh_order:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
