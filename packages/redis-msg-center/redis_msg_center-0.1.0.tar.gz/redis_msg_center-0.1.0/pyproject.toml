[project]
name = "redis-msg-center"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lithiumice", email = "hualinliang@outlook.com" }
]
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "sounddevice>=0.5.2",
    "numpy",
    "opencv-python>=4.11.0.86",
    "redis>=6.2.0",
    "pyaudio>=0.2.14",
]

[project.scripts]
redis-msg-center = "redis_msg_center:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
