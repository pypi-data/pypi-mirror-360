[project]
name = "evochi"
version = "0.1.3"
description = "The client implementation for the Evochi project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["grpcio", "grpcio-tools", "zstandard"]
