[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-trace"
version = "0.1.0"
description = "Tracing middleware for FastMCP with pluggable adapters."
authors = [
    { name="Your Name", email="akshay@contexaai.com" }
]
readme = "README.md"
requires-python = ">=3.8" 