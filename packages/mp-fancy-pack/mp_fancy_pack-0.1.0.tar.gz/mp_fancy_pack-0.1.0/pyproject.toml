[project]
name = "mp_fancy_pack"
version = "0.1.0"
description = "Codespring LLM Training Part 2"
readme = "README.md"
authors = [
    { name = "Péter Magyarosi", email = "magyarosi.peter@codespring.ro" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mp_fancy_pack = "mp_fancy_pack:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mp_fancy_pack"]
