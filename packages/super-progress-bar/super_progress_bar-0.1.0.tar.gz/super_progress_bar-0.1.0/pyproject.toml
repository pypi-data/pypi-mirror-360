[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "super_progress_bar"
version = "0.1.0"
description = "A colorful terminal progress bar with ETA and speed display"
authors = [
    { name="Davi Luiz Carneiro da Cunha", email="daviluizradical10@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.6"
readme = "README.md"
