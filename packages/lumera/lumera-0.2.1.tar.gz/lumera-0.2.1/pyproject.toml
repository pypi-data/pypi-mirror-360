[project]
name = "lumera"
version = "0.2.1"
description = "Lumera Agent SDK for API interaction and dynamic UI generation."
requires-python = ">=3.8"
dependencies = [
    "requests",
    "python-dotenv",
]

[tool.setuptools]
packages = ["lumera"]
