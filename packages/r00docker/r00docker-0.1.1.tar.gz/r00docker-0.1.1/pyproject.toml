[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00docker"]

[project]
name = "r00docker"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = [
    "docker==7.1.0",
    "r00logger",
    "r00secret",
]

