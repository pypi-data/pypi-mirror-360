[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "importune"
version = "0.0.0"
description = "Name reserved – functional release to follow."
authors = [{name = "Idin"}]
requires-python = ">=3.8"
