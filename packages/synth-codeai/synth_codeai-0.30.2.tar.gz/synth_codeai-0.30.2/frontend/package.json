{
  "name": "frontend-monorepo",
  "private": true,
  "workspaces": [
    "common",
    "web",
    "vsc"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev:web": "npm --workspace @synth-codeai/web run dev",
    "build:prebuilt": "rm -rf ../synth_codeai/server/prebuilt && npm --workspace @synth-codeai/web run build -- --outDir ../../synth_codeai/server/prebuilt"
  }
}
