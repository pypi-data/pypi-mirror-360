[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00secret"]

[project]
name = "r00secret"
version = "0.1.3"
requires-python = ">=3.10"
dependencies = [
    "r00logger",
    "python-box>=7.3.2",
    "ruamel.yaml"

]

