[build-system]
requires = ['setuptools>=42']
build-backend = 'setuptools.build_meta'

[project]
name = "OASIS-TUI"
version = "0.0.1"
authors = [
  { name="Oliver Zobel", email="oliver.zobel@tum.de" },
]
description = "Python TUI for the Open Acquisition System for IEPE Sensors (OASIS)"
dependencies = [
  "PySerial",
  "scipy",
  "numpy",
  "matplotlib",
]
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
oasis-tui = "oasis_tui:oasistui.run"

[project.urls]
Homepage = "https://gitlab.com/oasis-acquisition/oasis-tui"
Issues = "https://gitlab.com/oasis-acquisition/oasis-tui/-/issues"