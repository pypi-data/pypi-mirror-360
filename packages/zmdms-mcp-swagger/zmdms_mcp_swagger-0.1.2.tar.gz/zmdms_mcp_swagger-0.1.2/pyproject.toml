[project]
name = "zmdms-mcp-swagger"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.4",
]
[project.scripts]
zmdms-mcp-swagger = "zmdms_mcp_swagger.main:app"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"zmdms_mcp_swagger" = ["py.typed"]