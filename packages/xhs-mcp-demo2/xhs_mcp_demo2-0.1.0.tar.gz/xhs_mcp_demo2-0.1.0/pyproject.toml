[project]
name = "xhs-mcp-demo2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
xhs-mcp-demo2 = "xhs_mcp_demo2:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
