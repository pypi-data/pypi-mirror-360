[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "napari-flowering-apple-tree-pixel-classification"
version = "0.0.1"
readme = "README.md"

dependencies = []
