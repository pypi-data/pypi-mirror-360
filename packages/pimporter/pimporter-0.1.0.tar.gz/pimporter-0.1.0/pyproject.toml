[project]
name = "pimporter"
version = "0.1.0"
description = "Checks for conflicting, unused or duplicate import aliases"
authors = [{name="MWB"}]
dependencies = ["colorama"]

[project.scripts]
alias-checker = "pimporter.main:main"






