from setuptools import setup, find_packages

setup(
    name="cardinal-cli",
    version="0.0.1",
    description="Reserved name placeholder package",
    packages=find_packages(),
    install_requires=[],
)