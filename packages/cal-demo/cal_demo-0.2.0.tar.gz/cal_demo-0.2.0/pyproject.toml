[project]
name = "cal-demo"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wangjunbin", email = "wangjunbin@zhongan.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
    "requests>=2.32.4",
]

[project.scripts]
cal-demo = "cal_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
