[project]
name = "smthrandom"  # Use lowercase, hyphens are ok
version = "0.2.0"
description = "A random useless library"
readme = "README.md"
authors = [
  { name="Jason N", email="jason.vbnguyen@gmail.com" }
]
license = {text = "MIT"}  # Or reference a file: {file = "LICENSE"}
dependencies = ["random"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
