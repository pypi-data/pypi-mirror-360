[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "windowlicker"
version = "0.0.1"
description = "A cross-platform graphical library"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Warren Elrod", email = "you@example.com" }
]
license = { text = "MIT" }
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent"
]

[tool.setuptools.packages.find]
include = ["windowlicker"]
