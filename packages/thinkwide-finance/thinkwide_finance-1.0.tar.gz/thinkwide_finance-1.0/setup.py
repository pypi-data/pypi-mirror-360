# setup.py
from setuptools import setup, find_packages

setup(
    name='thinkwide-finance',
    version='1.0',
    packages=find_packages(),  # 自动查找包和子包
    install_requires=[],  # 依赖项列表（如果有的话）
)