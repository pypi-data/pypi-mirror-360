[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "emplace"
version = "0.1.0"
homepage = "https://github.com/eeemoon/emplace"
description = "Modern and easy way to handle placeholders."
authors = ["EeeMoon"]
license = "MIT"
readme = "README.md"
include = ["pyproject.toml"]

[tool.poetry.dependencies]
python = "^3.10"