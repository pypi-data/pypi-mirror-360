[project]
name = "gxl-portal-gun"
version = "0.1.0"
description = ""
authors = [
    {name = "udo-bit",email = "enote_gxl@163.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer (>=0.16.0,<0.17.0)",
    "pandas (>=2.3.0,<3.0.0)"
]

[tool.poetry]
packages = [{include = "gxl_portal_gun", from = "src"}]

[tool.poetry.scripts]
gxl-portal-gun = "gxl_portal_gun.main:app"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

