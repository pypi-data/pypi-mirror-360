[project]
name = "py-resource-man"
version = "0.1.0"
description = "The mans-at-work resource-man integration lib for Python."
authors = [{ name = "Otávio Abreu dos Santos Silva", email = "otavio.abreu96@gmail.com" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.11.7",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["maw_sdk_resource_client", "maw_sdk_resource_models"]
