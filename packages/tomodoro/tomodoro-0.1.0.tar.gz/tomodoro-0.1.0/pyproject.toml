[project]
name = "tomodoro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jdhalbert", email = "jdhalbert@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
tomodoro = "tomodoro:main.main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
