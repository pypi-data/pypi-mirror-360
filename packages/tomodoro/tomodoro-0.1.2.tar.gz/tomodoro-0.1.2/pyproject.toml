[project]
name = "tomodoro"
version = "0.1.2"
description = "A TUI pomodoro timer with no dependencies."
readme = "README.md"
authors = [
    { name = "jdhalbert", email = "tomodoro@rmdir.dev" }
]
license = { text = "MIT" }
requires-python = ">=3.13"
dependencies = []

[project.scripts]
tomodoro = "tomodoro:main.main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
