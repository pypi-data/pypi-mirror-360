[project]
name = "swtmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
swtmcp = "swtmcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
