# ETRAP SDK Environment Variables Example
# Copy this file to .env and update with your values

# AWS Credentials for S3 Access (Optional)
# If not set, the SDK will use AWS credentials from your system's default locations:
# - ~/.aws/credentials
# - IAM role (if running on EC2/ECS/Lambda)
# - Environment variables set elsewhere
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-west-2

# Note: The ETRAP SDK does not read any other environment variables.
# All configuration (organization ID, network, timeouts, etc.) must be
# passed directly to the ETRAPClient constructor in your code.