services:
  docker-status-mqtt-homeassistant:
    build:
      context: .
      platforms:
        - "linux/amd64"
        - "linux/arm64"
    container_name: docker-status-mqtt-homeassistant
    env_file:
      - .env
    restart: always
