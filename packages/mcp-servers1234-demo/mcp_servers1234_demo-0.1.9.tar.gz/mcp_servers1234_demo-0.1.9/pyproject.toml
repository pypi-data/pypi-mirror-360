[project]
name = "mcp-servers1234-demo"
version = "0.1.9"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
mcp-servers1234-demo = "mcp_servers1234_demo.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
