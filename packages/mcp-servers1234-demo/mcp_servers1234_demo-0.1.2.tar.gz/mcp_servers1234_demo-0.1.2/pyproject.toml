[project]
name = "mcp-servers1234-demo"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
dongzc1025-mcp-demo = "mcp_servers1234_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
