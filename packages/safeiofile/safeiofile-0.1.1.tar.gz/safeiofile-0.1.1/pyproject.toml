[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "safeiofile"
version = "0.1.1"
description = "Safely perform file I/O operations in Python — with backups and auto-rotation"
authors = [{ name = "Omkar D. Dorugade", email = "omkardorugade007@email.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/omkar086/safeiofile_Lib"
