[project]
name = "cleandog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pratik Shrivastava", email = "pratikpradeep1120@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cleandog = "cleandog:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
