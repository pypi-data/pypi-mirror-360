[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mailwizard"                         
version = "0.1.0"                       
description = "Simple library for sending emails via Python"
readme = "README.md"                     
requires-python = ">=3.7"                
license = { text = "MIT" }                # License type

authors = [
    { name = "Syed Abdullah Hassan", email = "syedabdullahhassann@gmail.com" }
]

dependencies = []                         # No external deps

[project.urls]
Homepage = "https://github.com/SyedAbdullahh/mailwizard"
