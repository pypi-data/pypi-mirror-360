[project]
name = "dfrobot-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "falconxu", email = "falcon.x@163.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
dfrobot-mcp = "dfrobot_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
