[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
script-files = ["scripts/reallyslowarg"]

[project]
name = "actuallyslowarg"
version = "0.1.0"
description = "A really slow CLI tool for testing tab completion with only argcomplete (no fastargsnap)"
authors = [
    {name = "Uday Ayya", email = "uday.ayya@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = ["cli", "argparse", "argcomplete", "tab-completion", "performance", "comparison"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Shells",
    "Topic :: Utilities",
]
dependencies = [
    "argparse",
    "argcomplete",
    "matplotlib",
    "numpy",
    "pandas",
    "scikit-learn",
    "requests",
    "click",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "flake8",
]

[project.urls]
Homepage = "https://github.com/udayayya/actuallyslowarg"
Repository = "https://github.com/udayayya/actuallyslowarg"
Issues = "https://github.com/udayayya/actuallyslowarg/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["reallyslowarg*"]
