[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00adb"]

[project]
name = "r00adb"
version = "0.1.2"
requires-python = ">=3.10"
dependencies = [
    "r00pykit",
    "cryptography",
    "pycryptodome",
    "rsa",
    "dsnparse",
]
