--help      -h 显示帮助信息
--version   -v 显示版本信息
--dir       -d 指定项目根目录名（默认 spring_demo）
--java      -j 指定 java 版本（默认21）
--tyhpe     -t 指定项目类型（maven-project/gradle-project，默认maven）
--packging  -p 打包方式，（jar/war，默认jar）
--dependencies -D 项目依赖（默认 spring-web, lombok）
--keepZip   -k 保留从官网下载的压缩包（默认不保留）
--packageName -P 项目包名（默认 com.example.demo）
