[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/r00textutil"]

[project]
name = "r00textutil"
version = "0.1.4"
requires-python = ">=3.10"
dependencies = [
    "r00logger",
    "r00system",
    "tqdm",
    "rich",
]

